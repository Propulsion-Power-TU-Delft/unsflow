# Grid Module
The Grid module contains the methods and classes to perform post-processing of turbomachinery 3D CFD results.
The inputs are the CFD and geometry data of the machine (hub, shroud, blade files), and the output are the 
meridional grid field data. The output files can be directly used by the Sun Module for instability prediction.